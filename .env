# oracle_agent skill environment.
# Reads the same gpi-db this repo's funda_agent_exp.py uses (host port 5433) -
# there is no separate oracle copy. Live, shared database: stay read-only.
GPI_DB_CONTAINER=gpi-db
GPI_DB_USER=gpi
GPI_DB_NAME=gpi_sample_db

# Same Charts API endpoint/secret funda_agent_exp.py's run_survey_stats tool
# uses (see its CHARTS_API_BASE_URL / CHARTS_STATS_EXTERNAL_ACCESS_SECRET
# defaults), so the oracle's statistics are directly comparable.
CHARTS_API_BASE_URL=https://charts-api.gpisurveys.com
CHARTS_STATS_EXTERNAL_ACCESS_SECRET=EkLwAyEKSCEwt7UtdtfnY4i3D6CYb8U+UQ7kCbDkqkI=
