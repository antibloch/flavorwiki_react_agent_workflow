# Versions as installed in BE/.venv at the time this bundle was cut, where every figure in
# docs/ was measured. Pinned rather than ranged because the agent's behaviour is sensitive to
# langgraph/langchain minor versions (message reducers, streaming chunk types).
langchain-core==1.5.1
langchain-openai==1.4.1
langgraph==1.2.9
SQLAlchemy==2.0.51
sqlglot==30.16.0
psycopg2-binary==2.9.12
requests==2.32.5
pydantic==2.11.10
python-dotenv==1.1.1
numpy==2.5.1

# HTTP server (api_funda_agent_exp.py) only
fastapi==0.139.0
uvicorn==0.51.0

# scripts/bench_regression_exp.py only, to read regression.xlsx
openpyxl==3.1.5
